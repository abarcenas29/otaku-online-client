// import { fromJS } from 'immutable';
// import { makeSelectItemAddDomain } from '../selectors';

// const selector = makeSelectItemAddDomain();

describe('makeSelectItemAddDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
