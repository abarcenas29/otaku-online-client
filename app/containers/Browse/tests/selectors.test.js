// import { fromJS } from 'immutable';
// import { makeSelectBrowseDomain } from '../selectors';

// const selector = makeSelectBrowseDomain();

describe('makeSelectBrowseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
