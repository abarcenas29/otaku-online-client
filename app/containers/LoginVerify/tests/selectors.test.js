// import { fromJS } from 'immutable';
// import { makeSelectLoginVeirfyDomain } from '../selectors';

// const selector = makeSelectLoginVeirfyDomain();

describe('makeSelectLoginVeirfyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
