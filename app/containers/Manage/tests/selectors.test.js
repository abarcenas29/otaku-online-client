// import { fromJS } from 'immutable';
// import { makeSelectManageDomain } from '../selectors';

// const selector = makeSelectManageDomain();

describe('makeSelectManageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
