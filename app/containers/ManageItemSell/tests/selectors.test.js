// import { fromJS } from 'immutable';
// import { makeSelectManageItemSellDomain } from '../selectors';

// const selector = makeSelectManageItemSellDomain();

describe('makeSelectManageItemSellDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
