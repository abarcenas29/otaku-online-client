// import { fromJS } from 'immutable';
// import { makeSelectItemDomain } from '../selectors';

// const selector = makeSelectItemDomain();

describe('makeSelectItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
