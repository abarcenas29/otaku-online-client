import config from './config/config.json'

export const FB_APP_ID = config.fb_app_id
export const FB_APP_SECRET = config.fb_app_secret
export const BASE_URL = config.base_url
export const API_URL = config.api_url
